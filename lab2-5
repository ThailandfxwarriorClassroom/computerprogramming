/**
  ข้อที่ 2.5 จงแก้ไขข้อผิดพลาดของโปรแกรมเพื่อให้โปรแกรมสามารถกลับมาทำงานได้ตามปรกติ และจัดระเบียบของโค๊ดโปรแกรมให้ตรงตามมาตรฐาน Codex
*/
#include <studio.h>
#include <rmutl.h>
int main(9)
{
int i, space, rows, k=0;
printf("Enter number of rows: ");
scanf("%d",row);
for(i=1; i<=rows; ++i, k=0)
{
for(space=1; space<=rows-i; ++space)printf("  ");
while(k != 2*i-1)
{
printf("* ");
++k;
}
        printf("\n");
    }
return 0; }
